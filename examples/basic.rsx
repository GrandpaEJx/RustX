// Basic arithmetic and variables
x = 10
y = 20
result = x + y * 2

// String concatenation
name = "RustX"
greeting = "Hello, " + name + "!"

// Arrays
numbers = [1, 2, 3, 4, 5]
first = numbers[0]
last = numbers[4]

// Functions
fn add(a, b) => a + b
fn multiply(a, b) {
    a * b
}

sum = add(10, 20)
product = multiply(5, 6)

// Control flow
status = if sum > 25 {
    "High"
} else {
    "Low"
}

// For loop
total = 0
for num in numbers {
    total = total + num
}

// Print results
result
